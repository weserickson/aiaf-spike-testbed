# throwaway spikes repo

Exists only to test the pod boot path for `aiaf-spike-dockerimage`. Not the real
experiments repo — that is `agencyenterprise/aiaf-fellowship-p2-selfie`, which holds
code, specs and result notebooks.

There is deliberately no `CLAUDE.md` here. The standing instructions ship with the
container image and are installed at `~/.claude/CLAUDE.md` at boot.

Safe to delete once the automation is trusted.
